# Bochs WASM PoC - راهنمای اجرای تست

## فایل‌های خروجی

| فایل | مسیر | حجم | وضعیت |
|------|------|------|--------|
| bochs_x64.wasm | `/workspace/bochs-wasm-poc/poc-test/bochs_x64.wasm` | 2.8 MB | ✅ VERIFIED |
| bochs_x64.js | `/workspace/bochs-wasm-poc/poc-test/bochs_x64.js` | 110 KB | ✅ VERIFIED |
| index.html | `/workspace/bochs-wasm-poc/poc-test/index.html` | 6.5 KB | ✅ VERIFIED |
| BIOS-bochs-latest | `/workspace/bochs-wasm-poc/poc-test/BIOS-bochs-latest` | 128 KB | ✅ VERIFIED |
| VGABIOS-lgpl-latest | `/workspace/bochs-wasm-poc/poc-test/VGABIOS-lgpl-latest` | 38 KB | ✅ VERIFIED |
| test_boot.iso | `/workspace/bochs-wasm-poc/poc-test/test_boot.iso` | 1.0 MB | ✅ Bootable ISO created |

## دستور Build

```bash
cd /workspace/bochs-wasm-poc
source ./emsdk/emsdk_env.sh

cd build-bochs
emconfigure ../bochs-src/bochs/configure \
  --host wasm32-unknown-emscripten \
  --enable-x86-64 \
  --with-nogui \
  --enable-usb --enable-usb-ehci \
  --disable-large-ramfile --disable-show-ips --disable-stats \
  --disable-logging \
  --enable-repeat-speedups --enable-fast-function-calls \
  --disable-trace-linking --enable-handlers-chaining --enable-avx

CFLAGS="-O2 -s WASM=1 -s ASYNCIFY=1 -s ALLOW_MEMORY_GROWTH=1 \
  -s TOTAL_MEMORY=$((30*1024*1024)) -sNO_EXIT_RUNTIME=1 \
  -sFORCE_FILESYSTEM=1 -D__GNU__" \
CXXFLAGS="${CFLAGS}" \
emmake make -j$(nproc) bochs

cp bochs_x64.wasm bochs_x64.js ../poc-test/
```

## نسخه Emscripten

```
emcc (Emscripten gcc/clang-like replacement + linker emulating GNU ld) 3.1.40
```

## روش اجرای PoC در Browser

### مرحله 1: راه‌اندازی HTTP Server

```bash
cd /workspace/bochs-wasm-poc/poc-test
python3 -m http.server 8765
```

### مرحله 2: باز کردن در Browser

آدرس زیر را در browser باز کنید:
```
http://localhost:8765/
```

### مرحله 3: انتخاب فایل‌ها

در صفحه وب:
1. فایل BIOS را انتخاب کنید: `BIOS-bochs-latest`
2. فایل VGABIOS را انتخاب کنید: `VGABIOS-lgpl-latest`
3. فایل ISO را انتخاب کنید: `test_boot.iso` (یا هر ISO بوت‌شونده دیگر)

### مرحله 4: شروع emulation

روی دکمه "Start Bochs" کلیک کنید.

### مرحله 5: مشاهده خروجی

خروجی Bochs در قسمت console صفحه نمایش داده می‌شود. باید پیام‌های زیر را ببینید:

```
Bochs WASM loaded successfully
Writing BIOS to virtual filesystem...
BIOS written (131072 bytes)
VGABIOS written (38400 bytes)
ISO written (1048576 bytes)
bochsrc created
Starting Bochs emulation...
--- BOCHS BOOT LOG ---
[Bochs BIOS messages...]
[Boot from CD-ROM...]
```

## تأییدیه‌های خودکار

### 1. وجود فایل‌ها
✅ bochs_x64.wasm (2,856,211 bytes)
✅ bochs_x64.js (110,211 bytes)
✅ index.html (6,550 bytes)
✅ BIOS-bochs-latest (131,072 bytes)
✅ VGABIOS-lgpl-latest (38,400 bytes)
✅ test_boot.iso (1,048,576 bytes)

### 2. اعتبارسنجی WASM
✅ Magic number: 0x00 0x61 0x73 0x6d (\0asm)

### 3. عدم استفاده از SharedArrayBuffer
✅ grep نتیجه: 0 match

### 4. عدم استفاده از pthreads
✅ grep نتیجه: 0 match

### 5. فعال بودن x86-64
✅ config.log: --enable-x86-64 flag present

## معماری Runtime ISO Boot

```
┌─────────────────┐
│  External ISO   │  (User selects file in browser)
└────────┬────────┘
         │ FileReader API
         ▼
┌─────────────────┐
│   JavaScript    │  (index.html + bochs_x64.js)
│  FS.writeFile   │
└────────┬────────┘
         │ Virtual File System
         ▼
┌─────────────────┐
│   WASM Memory   │  (/pack/boot.iso)
└────────┬────────┘
         │ Bochs ATA driver
         ▼
┌─────────────────┐
│  Bochs CD-ROM   │  (ata0-master: type=cdrom)
└────────┬────────┘
         │ INT 13h
         ▼
┌─────────────────┐
│      BIOS       │  (BIOS-bochs-latest)
└────────┬────────┘
         │ Boot sequence
         ▼
┌─────────────────┐
│  VGA/nogui      │  (Output to JS console)
└─────────────────┘
```

## محدودیت‌ها

1. **nogui Display:** فعلاً فقط خروجی متنی به console - برای VGA framebuffer نیاز به توسعه بیشتر دارد
2. **Speed:** Emulation سرعت پایینی دارد (مناسب برای PoC)
3. **Memory:** حافظه اولیه 30 MB تنظیم شده است
4. **Browser Test:** نیاز به تست دستی در مرورگر دارد (محیط headless فعلی امکان تست خودکار ندارد)

## قدم‌های بعدی

1. **Manual Browser Test:** باز کردن http://localhost:8765/ در browser و تست interactive
2. **Android WebView Test:** Deploy به Android app و تست در System WebView
3. **Framebuffer Extraction:** توسعه display_library برای استخراج VGA output
4. **Windows x64 ISO Test:** تست با Windows x64 installation ISO

## نتیجه‌گیری

✅ **Build کامل با موفقیت انجام شد**
✅ **فایل‌های WASM و JS معتبر هستند**
✅ **Single-threaded بدون pthread**
✅ **بدون SharedArrayBuffer**
✅ **x86-64 support فعال است**
✅ **مکانیزم Runtime ISO loading پیاده‌سازی شده است**

⚠️ **نیاز به تست تعاملی در browser دارد** (به دلیل محیط headless)
❌ **Android WebView هنوز تست نشده است**

---

**Generated by:** Bochs WASM PoC Team  
**Date:** 2024-09-03
